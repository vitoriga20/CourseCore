# 2026-08-06 工作日志

## 绘制 CourseCore 架构图与时序图

为用户用 diagram-builder skill 绘制了 3 张图，已保存为 SVG 源文件到 `diagrams/`：
- `architecture.svg` — 系统架构图：客户端 Vite SPA → 边缘层(Cloudflare Pages/CDN + BFF Hono) → 数据层(Supabase + KV)。含构建期打包通道、anon key 直连降级通道。
- `sequence-answer-sync.svg` — 答题判分与进度/错题同步时序图（学生浏览器/路由/校验器/客户端服务/BFF/Supabase）。
- `sequence-content-load.svg` — 试卷题库内容加载时序图（视图/practice-data/BFF content/Supabase/静态 bundle），含 BFF 可用 vs 静态 fallback 的 alt 框。

### 关键架构事实（摸清的真实状态，超出旧 MEMORY.md）
- 项目已从「纯静态 SPA」演进为 **混合架构**：既有构建期 `scripts/fetch-from-supabase.js` 把内容写死进 `src/data/*.js`（examPapers 404KB / questions 211KB），又有运行时 BFF。
- BFF：`bff/`（Hono，TS 源码 `bff/src/*.ts`，已 build 到 `bff/dist`）+ `functions/api/[[route]].js`（Cloudflare Pages Functions 挂载 Hono app）。路由：`/api/v1` health/content、`/api/v1/me` user、`/api/v1` judge、`/api/v1/leaderboard`。持有 service_role（仅服务端，经 wrangler secret/.dev.vars 注入）。
- 前端服务层：`src/services/apiClient.js`(封装 /api/v1 + JWT)、`sync.js`(pushAnswer/pushItemProgress/wrong-book，BFF 失败 fallback 直连 Supabase anon)、`practice-data.js`(试卷加载，优先级 内存→localStorage→BFF→静态 EXAM_PAPERS)、`content.js`(运行时读 theory/questions/kp)、`auth.js`(Supabase Auth 会话)。
- `backend-architecture-redesign.md` 是目标架构（Phase 0-3），当前仅部分落地（BFF 已建，业务权威上移 Phase 2 未完成）。
- 注意：show_widget 沙箱禁止字面串 "localStorage"/"sessionStorage"，SVG 标签里要避开（改用「本地存储」等措辞）。
