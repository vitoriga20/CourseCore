# CourseCore 环境变量示例
# 复制本文件为 .env.local 并填入真实值（.env.local 已加入 .gitignore，不会提交）。

# MinerU API token：仅用于本地构建时从 PDF 抽取训练题，不会进入前端产物。
# 获取地址：https://mineru.net/apiManage/token
# MINERU_TOKEN=your_mineru_token_here

# Supabase 配置：用于用户认证与云端进度同步。
# URL 格式：https://<project-id>.supabase.co
# Anon Key 必须是 public anon key（eyJ... 开头），严禁使用 service role key（sb_secret_...）。
# VITE_SUPABASE_URL=https://<project-id>.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJ...

# Google Analytics 测量 ID（可选）
# VITE_GA_ID=G-XXXXXXXXXX
