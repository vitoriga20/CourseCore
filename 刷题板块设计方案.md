> 文档状态：产品/交互设计定稿，部分能力已实现；数据模型、后台写入和 BFF 边界仍需与当前代码核对。
>
> 本文是目标体验说明，不是当前功能完成证明。

# CourseCore 刷题板块设计方案

> 版本：v1.0（设计定稿） | 日期：2026-08-03
> 设计稿：https://ardot.tencent.com/file/710181938468190

---

## 一、项目概述

CourseCore 是大学基础课学习平台（Vite + 原生 JS + Tailwind + Supabase）。本方案新增 **刷题板块**，覆盖：按试卷/按题型双模式刷题、即时/提交后两种评判、知识库（错题库 + 收藏）、社区（刷题技巧论坛）、我的刷题记录、我的试卷组卷等完整闭环。

**设计原则**：绿色主题（`#16A34A` / `#2DD288` / `#142A1F`）、深色背景（`#0E0E16`）、卡片化布局、多栏信息架构（拒绝整宽列表铺满）、复用现有综合测验答题组件。

---

## 二、信息架构

```
首页（4 tab）
├── 学习板块（现有）
├── 刷题板块 ★
│   ├── 概览页（Screen 1）
│   │   ├── 4 指标卡（2×2 网格）+ 添加我的试卷按钮
│   │   ├── 排行榜 + 最近练习（两栏）
│   │   └── 底部：模式切换 + 进入刷题中心 CTA
│   ├── 刷题中心·按试卷（Screen 2）
│   ├── 刷题中心·按题型（Screen 10）
│   ├── 提交后总结（Screen 3）
│   └── 添加我的试卷弹窗（Screen 11，底部 Slider 三栏）
├── 知识库板块
│   ├── 错题库（Screen 4）
│   ├── 收藏（Screen 6）
│   └── 复盘会话（Screen 5）
├── 社区板块（新 tab）
│   ├── 文章列表（Screen 7）
│   └── 文章详情（Screen 8）
└── 我的页面
    └── 刷题记录（Screen 9）
```

---

## 三、页面设计

### Screen 1 刷题板块概览页
```
┌────────────────────────────────────────────────────────┐
│ 刷题板块  [按试卷刷题|按题型刷题]   (模式切换)           │
│ ┌──────────┬──────────┬───────────────┐                │
│ │ 12 套数  │ 248 题量 │  ➕ 添加我的试卷 │                │
│ ├──────────┼──────────┤  从题库选题组卷  │                │
│ │ 82% 正确 │ 12.5h 耗时└───────────────┘                │
│ └──────────┴──────────┘                                │
│ ┌────────────────────┬────────────────────┐            │
│ │ 排行榜 [试卷数][题量]│ 最近练习           │            │
│ │ [正确率][耗时]      │ 高数A2·2019        │            │
│ │ 1 张同学 6套        │ 耗时45分·正确70%   │            │
│ │ 2 李学长 5套        │ 高数·单选组卷      │            │
│ │ 3 王同学 4套        │ 耗时28分·正确83%   │            │
│ │ 我 第12名·2套       │                    │            │
│ └────────────────────┴────────────────────┘            │
│ [进入刷题中心 →]  (底部 CTA)                            │
└────────────────────────────────────────────────────────┘
```

- **4 指标卡**（语义）：累计试卷套数 = 刷完的试卷数；累计题目数量 = 提交过答案的题数；平均正确率；刷题总耗时
- **排行榜**：4 个榜 tab 独立排名（试卷数/题量/正确率/耗时），无综合榜；用户为登录用户，自己的行绿色高亮
- **最近练习**：按试卷刷题 → 试卷名；按题型刷题 → 「学科+题型组卷」名；精简三项（名称 + 耗时 + 正确率）
- **添加我的试卷**：绿边大按钮，点击弹出 Screen 11

### Screen 2 刷题中心 · 按试卷
```
顶栏：← 返回刷题 | 高数A2·2019 |（预览态无进度/提交）
┌─────────────┬───────────────────────────────┐
│ 试卷索引    │ 试卷预览                      │
│ ▸高等数学 2套│ 高数A2·2019年期末 [未开始]     │
│  ●高数A2 2019│ 20题 | 120分钟 | — 上次成绩   │
│  高数A1 2020│ ▓▓▓▓▓▓▓░░░ 单选10·填空6·解答4 │
│ ▸线代 3套    │ [进入试卷 →]                  │
│ ▸大物 4套   │                                │
└─────────────┴───────────────────────────────┘
```
- 左侧：学科 → 试卷 二级树；点「进入试卷」→ **复用综合测验页面**（quizSession），题目用试卷的题
- 右侧预览：题型构成条 + 题量 + 时长 + 上次成绩 + 状态

### Screen 3 提交后总结
- 复用刷题中心布局，左侧索引标记「✓ 已提交」
- 成绩卡：正确率大数字 + 成绩徽章（优秀≥90/良好≥75/加油<75）+ 对/错统计 + 进度条
- 错题复盘（简洁版）：**题号胶囊列表**（点击跳错题库对应题）+ 全宽「打开错题库」按钮
- 重刷逻辑：全新清空答案，保存每次总结记录，不保存刷题过程（中途退出进度丢失）

### Screen 4 知识库 · 错题库
```
知识库 [⚙齿轮设置]    ← 点击旋转+展开局部下拉（曲线模式）
(描述)
[错题库 | 收藏]        ← tab
[高等数学|线性代数|大学物理]  ← 学科切换器（每科独立知识库）
┌──────────────────┬──────────────────┐
│ 复习队列(小卡片)  │ 掌握度分析       │
│ [单选·复习中·计算失误] [原因|考点]   │
│ [填空·未掌握·概念不清]  ◇雷达图     │
│ [解答·复习中·审题错误]             │
└──────────────────┴──────────────────┘
[已选 N 题 · 开始复盘]
```
- **右上角齿轮**：复习曲线设置（经典 1-2-4-7-15 / 紧凑 1-2-4），下拉局部卡片
- **复习原因** 5 类预设：概念不清 / 计算失误 / 审题错误 / 方法不熟 / 时间不够（雷达图 Tab1 分类）
- **雷达图**：Tab1 按错误原因 / Tab2 按考点
- **知识库按学科独立**：`wrong_book` 按学科隔离

### Screen 5 错题复盘会话
- 复用综合测验答题组件
- **固定交互**：每做一题自动评判 + 解析点击「查看解析」才展开（不受 answer_reveal 影响）

### Screen 6 知识库 · 收藏
- 四分类筛选：全部 / 理论 / 题目 / 试卷 / 文章
- 卡片列表：图标 + 分类标签 + 来源 + 标题 + 操作（刷题/阅读）+ 黄星取消收藏
- 理论、题目、试卷（含自组卷）、文章均可收藏

### Screen 7 / 8 社区
- 列表：作者头像 + 昵称 + 时间 + 分类标签 + 标题 + 摘要 + 收藏数 + 讨论数 + 阅读全文
- 详情：作者区 + 大标题 + Markdown 正文 + 底部收藏/讨论/分享
- 发帖：普通用户审核制；管理员直接发布；文章存 Supabase

### Screen 9 我的 · 刷题记录
- 3 统计卡（已刷试卷/累计做题/正确率）+ 筛选（全部/进行中/已完成）
- 记录卡：试卷名 + 状态徽章 + 题数统计 + 耗时 + 正确率 + 查看错题/继续刷题
- **记录明细**：每次提交保存总结记录，可展开查看每题对错

### Screen 10 刷题中心 · 按题型
- 左侧索引：学科 → 题型 二级树（高数：单选20/多选8/填空16/解答10；线代：单选12/填空6；大物：单选18/填空10/解答8）
- 右侧预览：「高数 · 单选题组」+ 来源「6 套试卷·跨卷抽题」+ 题量/时长/上次成绩 + 构成条 + 进入练习

### Screen 11 添加我的试卷弹窗（底部 Slider 三栏）
```
添加我的试卷                    [已选 N 题]  [创建试卷]
┌────────┬──────────────────────┬────────────────────┐
│ 目录    │ 高数A2·2019年期末    │ 试卷预览(综合测验)  │
│[按试卷] │ ┌────────┬────────┐ │ [顺序] 高数A2·综合 │
│ [按题型]│ │▣单选a,b│填空∫x² │ │ ▓▓▓▓░(进度条)     │
│ ▸高数A2 │ │  (选中)│       │ │ [第1/5题] 设a,b,c  │
│  高数A1 │ ├────────┼────────┤ │ ●A (a·c)b-(b·a)c  │
│  线代   │ │ 解答求极值│多选不可导│ │ ○B b-(a·b/a·a)a  │
│  大物   │ └────────┴────────┘ │ 题号 [1][2][3][4][5]│
│         │ 已选题目(拖拽排序)   │ [上一题] [下一题]   │
│         │ ⠿1 设a,b,c…         │                    │
│         │ ⠿2 计算∫x²dx…       │                    │
└────────┴──────────────────────┴────────────────────┘
```
- **最左栏**：按试卷 / 按题型 tab + 列表（筛选）
- **中栏**：题目**卡片网格**（点击卡片=选中，绿边高亮，无勾选框）+ 下部**已选题目拖拽排序**（题号 + 题干 + 拖拽把手）
- **右栏**：综合测验小窗口实时预览（同步选中题目，还原 quizSession 真实布局）
- 确认后：命名 → 保存「我的试卷」→ 刷题中心筛选栏「我的试卷」分组下

---

## 四、数据模型（Supabase）

### 现有表扩展
```sql
-- 题目级答案显示时机（综合测验/刷题中心/收藏刷题统一读取）
ALTER TABLE exam_questions ADD COLUMN answer_reveal TEXT DEFAULT 'after_submit'; -- 'instant' | 'after_submit'
```

### 新增表
```sql
-- 收藏（理论/题目/试卷/文章 通用）
CREATE TABLE favorites (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  ref_type TEXT,          -- 'theory' | 'question' | 'exam' | 'article'
  ref_id TEXT,
  created_at TIMESTAMPTZ,
  UNIQUE (user_id, ref_type, ref_id)
);

-- 错题库（艾宾浩斯 + Shiroha 精细模型）
CREATE TABLE wrong_book (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  question_id TEXT,
  subject_id TEXT,          -- 知识库按学科独立
  curve_type TEXT DEFAULT 'classic',  -- 'compact' | 'classic'（全局配置）
  stage INT DEFAULT 0,                 -- 复习阶段 = reviewLevel
  wrong_count INT DEFAULT 0,
  right_count INT DEFAULT 0,
  streak_correct_count INT DEFAULT 0,  -- 连续答对（关键）
  status TEXT DEFAULT '未掌握',        -- 未掌握/复习中/已掌握
  reason TEXT,                         -- 复习原因（5 类预设之一）
  last_wrong_at TIMESTAMPTZ,
  last_correct_at TIMESTAMPTZ,
  last_reviewed_at TIMESTAMPTZ,
  next_review_at TIMESTAMPTZ,
  last_answer JSONB,
  UNIQUE (user_id, question_id)
);

-- 刷题技巧文章
CREATE TABLE posts (
  id UUID PRIMARY KEY,
  author_id UUID REFERENCES auth.users,
  title TEXT,
  content TEXT,             -- Markdown
  category TEXT,            -- 高数/线代/大物/方法
  status TEXT DEFAULT 'pending',  -- pending(待审)/published(管理员直接发布)
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);

-- 文章收藏
CREATE TABLE post_favorites (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  post_id UUID REFERENCES posts,
  created_at TIMESTAMPTZ,
  UNIQUE (user_id, post_id)
);

-- 我的试卷（自组卷）
CREATE TABLE my_papers (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users,
  name TEXT,
  question_ids JSONB,       -- 有序题目 id 数组
  created_at TIMESTAMPTZ
);
```

### 刷题记录（复用现有 completedQuestions + 新增总结记录）
- 每次提交保存一条总结记录（mode/试卷/题数/对错/正确率/耗时/明细），时间倒序
- 不保存答题过程（重刷全新清空；中途退出进度丢失）

---

## 五、核心逻辑

### 1. 答案显示时机（answer_reveal）
| 值 | 行为 | 场景 |
|---|---|---|
| `instant` | 每做一题即时评判 | 练习模式 |
| `after_submit`（默认）| 全做完点提交才出成绩 + 错题隐藏解析 | 模拟考试/刷题中心 |

**复盘会话例外**：固定「自动评判 + 解析点击展开」，不受字段影响。

### 2. 艾宾浩斯复习引擎（融合 Shiroha Quiz）
```
答错 → wrongCount+1, streak=0, stage=0, status=未掌握, 次日复习
答对 → streak+1, stage+1, status=复习中
毕业 → stage ≥ 曲线阶段数（紧凑3/经典5） 或 连续答对≥2 → 已掌握 → 自动移除
曲线切换「重合」：已完成阶段保留，按新曲线继续复习到毕业
复习间隔：经典 [1,2,4,7,15] / 紧凑 [1,2,4]（天）
```

### 3. 排行榜
- 4 个独立榜：试卷数 / 题量 / 正确率 / 耗时
- 数据源：所有登录用户的学习统计聚合；自己的行「我」高亮

### 4. 试卷预览 → 进入
- 右侧默认显示预览（题型构成/题量/时长/上次成绩/状态）
- 点「进入试卷/练习」→ 复用综合测验页面（quizSession），题目用试卷或按题型组卷

### 5. 我的试卷
- 底部 Slider 三栏弹窗：目录筛选 → 题目卡片点选 → 已选拖拽排序 → 综合测验实时预览
- 命名保存 → 刷题中心「我的试卷」分组；支持重刷/删除；读 answer_reveal

---

## 六、技术选型

| 用途 | 选型 | 说明 |
|---|---|---|
| 图表 | **ECharts** | 按需引入（折线/雷达/环形/柱状），内置 dark + 绿色主题 |
| Markdown 渲染 | marked（已有） | 社区文章 |
| Markdown 编辑 | easymde（已有） | 发帖编辑器 |
| 后端/数据库 | Supabase（已有） | 4 张新表 + 字段扩展 |
| 拖拽排序 | sortablejs（已有） | 已选题目排序 |
| 前端 | Vite + 原生 JS + Tailwind | 现有技术栈 |

---

## 七、实施计划（5 期）

1. **第一期**：DB 迁移（4 张新表 + `exam_questions.answer_reveal`）→ 知识库两栏骨架
2. **第二期**：收藏全类型接入（理论/题目/试卷/文章）+ ECharts 图表
3. **第三期**：错题库 + 艾宾浩斯复习引擎（含「重合」算法 + 复盘会话 + 今日复习）
4. **第四期**：我的刷题记录（含明细）+ 刷题中心已刷标记 + 排行榜
5. **第五期**：社区 MVP（发帖 + 列表 + 详情 + 收藏 + 审核）+ 我的试卷组卷

---

## 八、参考

- 错题库精细模型借鉴：[Shiroha Quiz 深度分析](D:\大学生好用的资源\刷题系统的学习借鉴\开源项目\ShirohaQuiz\analysis.md)（连续答对判定、状态机、间隔重复、记录明细）
- 综合测验页面结构：`src/views/quizSession.js`
