---
alwaysApply: true
---

# 平台适配规则：项目级智能体规范

本项目的唯一规范源是仓库根目录的 AGENTS.md。每次任务开始前和完成后，必须先读取并遵守该文件。

记忆文件的统一位置为项目根目录的 Memory/；不要把 .trae、.workbuddy 或其他平台目录作为新记忆的写入目标。

已有的平台私有记忆目录只作为历史兼容来源读取；新记忆必须写入 Memory/，并在必要时去重合并。

如果平台无法自动发现根目录 AGENTS.md，平台适配层只需要把 AGENTS.md 的内容注入当前任务上下文；适配层不得新增或改写另一套规则。
