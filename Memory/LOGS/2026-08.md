# 2026-08 工作日志

## [2026-08-10] 理论正文图/表占位符机制（后台标注 → 数据库 → 前端展示）
- 目标：后台对正文标注 `[图N:名称]`/`[表N:名称]`，自动补图、入库、前端替换展示。测试课程=物理"质点运动学基础"`p1b-m1-01`。
- 结果：全链路打通并过构建。`[图1:位置矢量]` 由 `content_figures` 表内联 SVG 替换展示。
- 关键决策：
  - 新增表 `public.content_figures`（item_id/placeholder/kind/alt/content），`kind` 区分 figure(svg)/table(html)，`UNIQUE(item_id,placeholder)`。
  - 前端理论渲染用"先换 token 再还原"：`[图N]`→临时 token→marked 渲染→还原 HTML，避免 marked 干扰；未入库占位符保留原文。
  - 数据链路：`content.js.loadTheoryContent` 与 `admin.js.getItemContent` 均返回 `figures`；`router.js` 内容相同但带 figures 也注入；后台预览 `adminPage.js` 新增 `renderTheoryPreviewBody` 解析。
  - 样式：`.cc-figure`/.cc-figure.cc-table 追加到 `style.css`（SVG 居中自适应、表格带边框）。
- 变更范围：`scripts/migrations/003-content-figures.sql`、`scripts/seed/05-content-figures-position-vector.sql`、`public/assets/figures/position-vector.svg`（新增）；`src/services/content.js`、`src/services/admin.js`、`src/router.js`、`src/views/practiceList.js`、`src/views/admin/adminPage.js`、`src/style.css`。
- 验证：`npm run build` 通过（CSS 79.55kB，旧 79.10kB）。
- 新增/更新记忆：DOMAINS 待建 content-figures.md（或并入 MEMORY.md）。
- 未解决风险：migration 003 与 seed 05 需在 Supabase 手动执行后方能生效；fetch-from-supabase 仍不拉 content_figures（前端运行时联查，无碍）。

## [2026-08-10] 收口仪式落地（AGENTS.md 拆分必做/可选）
## [2026-08-10] 今日复习"加载复盘题目中"卡死
- 目标：修复知识库 → 开始今日复习后一直停在"加载复盘题目中"。
- 结果：卡死恢复；并修复隐藏的渲染错误与 BFF 答案字段缺失。
- 关键决策：
  - 根因是 render 内同步 init，先于 DOM 写入，`getElementById` 拿 null 早退。改为 render 只返回 HTML、init 在 innerHTML 赋值后单独调用（照 practice-session 模式）。
  - 修好后暴露 snake_case 未转 camelCase：wrong_book 联表 questions 的 `question_type` 需在 quiz-adapter 转成 `questionType`，否则 `No renderer for view type "undefined"`。
  - BFF `/me/wrong-book` questions select 补 answer/answers/solution。
- 变更范围：`src/views/practice/review-session.js`、`src/router.js`、`src/views/practice/quiz-adapter.js`、`src/services/apiClient.js`（额外加 15s 超时防御，非根因）、`functions/api/[[route]].js`。
- 验证：浏览器实测加载文字消失不再卡；`npm run build` 通过；登录态完整答题界面待用户复测。
- 新增/更新记忆：`DOMAINS/review-session-loading.md`、`MEMORY.md`。
- 未解决风险：fetch-from-supabase 拉取 exam_sections 报 permission denied（构建时已观察到，非本次修复范围）。

## [2026-08-08] 错题驱动的今日复习主线落地
- 目标：把知识库首屏收敛为“开始今日复习”，并让每道错题在总结页必填可多选薄弱点。
- 结果：首屏改为任务优先的紧凑错题网格 + 薄弱点排行；复习入口直接使用完整错题队列；空状态引导去刷题。
- 关键决策：不展示时长预估；不提供“就是错了/粗心”选项；错题原因采用六项固定目录。
- 变更范围：知识库、复习会话、总结组件、错题持久化/BFF 校验、数据库迁移与契约测试。
- 验证：17 项 Node 测试通过；BFF typecheck、BFF bundle、Vite production build 通过（未配置 Supabase 时跳过数据拉取）。
- 新增/更新记忆：`DOMAINS/practice-session-sync.md`。
- 未解决风险：数据库迁移 SQL 需要在目标 Supabase 环境执行后才会启用 `wrong_book.reasons`。

## [2026-08-08] 全库题目答案/解析补全（science-answer-solver）
- 目标：为数据库 questions 表全部题目补齐 answer 与 solution。
- 结果：761 题全部具备答案与解析（has_answer=761，has_solution=761，bad_answer=0）。
- 关键决策：
  - 导出待处理 605 题，分 13 批（50 题/批）交子智能体生成并 sympy/numpy 验证。
  - 写入绕过 RLS：生成的批量 UPDATE SQL 经 execute_sql 直连执行，分 13 段（scripts/sqlseg/seg-01~13）。
  - 补漏 11 题：批量导出时子智能体只识别 title 字段，而 `q-exam-calculus-2-2022-*` 的公式在 title 中被剥离（只剩 `\_\_\_`），但 content 字段含完整题干；本轮基于 content 单独解题并验证写回。
- 变更范围：仅数据库 questions 表（answer/solution/updated_at），无代码改动。
- 验证：11 题补漏均过 sympy 数值/符号验证（线线垂直、隐函数偏导、可微性、二重积分、级数收敛、f_xx、法平面、奇函数积分、线积分、傅里叶收敛值、|cos| 重积分）。
- 新增/更新记忆：无长期事实变更（一次性数据维护）。
- 未解决风险：无。

## [2026-08-08] 后台期末试卷题目排序
- 目标：编辑期末试卷时，题目按原试卷顺序显示。
- 结果：按大题顺序后再按大题内题号排序；无大题的新式题目维持其保存顺序。
- 关键决策：在 `src/services/admin.js` 的数据服务层排序，统一编辑器与预览的输入顺序。
- 变更范围：`src/services/admin.js`、`tests/admin-exam-order.test.js`。
- 验证：`node --test tests/*.test.js`（8/8 通过）；Vite 生产构建通过。
- 新增/更新记忆：`DOMAINS/admin-exam-paper-ordering.md`、`MEMORY.md` 索引。
- 未解决风险：构建仅有既存的大体积 chunk 警告。

## [2026-08-08] 试卷编辑器排序兼容旧数据
- 目标：修复试卷编辑器左侧题目列表仍按各大题内序号交错显示的问题。
- 结果：缺失大题关联的旧题目会从题目 ID 后缀恢复“大题号 → 题号”顺序。
- 关键决策：排序逻辑仍集中在数据服务层，不在列表渲染层重复实现。
- 变更范围：`src/services/admin.js`、`tests/admin-exam-order.test.js`。
- 验证：旧编号排序回归用例先失败后通过；待全量测试与构建复核。
- 新增/更新记忆：更新 `DOMAINS/admin-exam-paper-ordering.md`。
- 未解决风险：未使用服务密钥读取生产数据；以回归用例覆盖已知旧编号格式。

## [2026-08-08] 后台预览 LaTeX 下标渲染
- 目标：修复试卷编辑器预览中部分 LaTeX 以源码显示的问题。
- 结果：Markdown 解析前保护完整数学片段，避免 `_` 被转换为 `<em>` 并打断 MathJax 公式。
- 关键决策：新增共享的 `renderMarkdownWithMath`，后台理论、练习和试卷预览共用该入口。
- 变更范围：`src/utils/markdown.js`、`src/views/admin/adminPage.js`、`tests/math-markdown.test.js`。
- 验证：截图中的 `\\left.\\frac{dz}{dx}\\right|_{x=0}` 回归用例先失败后通过；待全量测试与构建复核。
- 新增/更新记忆：`DOMAINS/math-rendering.md`、`MEMORY.md`。
- 未解决风险：无。

## [2026-08-08] 刷题中心交卷未收录错题
- 目标：修复已交卷的代数试卷未将错题写入错题中心的问题。
- 结果：恢复交卷完成回调注册，使其执行错题本与刷题记录同步。
- 关键决策：适配器直接返回底层 `quizSession` 状态，保持调用方以 `onFinish` 挂载后处理的既有边界。
- 变更范围：`src/views/practice/quiz-adapter.js`、`tests/practice-session-adapter.test.js`。
- 验证：回归测试先失败后通过；全量 Node 测试 11/11 通过；BFF 类型检查和 Vite 生产构建通过。
- 新增/更新记忆：`DOMAINS/practice-session-sync.md`、`MEMORY.md`。
- 未解决风险：线上已完成的历史试卷不会自动回填；需要重新作答后触发同步。

## [2026-08-08] 错题驱动的今日复习主线
- 目标：对齐知识库重构主线，缩短新用户从打开页面到开始复习的路径。
- 结果：确认首页以“今日复习”为唯一主任务；刷题总结页逐题必填可多选错因，作为复习与薄弱点分析的数据来源。
- 关键决策：不展示复习时长预估；不提供“就是错了”等无效错因；课程筛选和错题分析均为次级内容。
- 变更范围：设计方案与项目决策记录，尚未改动产品代码。
- 验证：用户逐项确认主线、直接进入第一题、无时长预估、错因多选必填及标签范围。
- 新增/更新记忆：`DECISIONS/2026-08-08-wrong-answer-review-mainline.md`、`MEMORY.md`。
- 未解决风险：待在实现计划中核查现有数据模型是否已支持逐题多选错因。
